# App Service Module

Responsibility:

- hosting design for `request-web` and `request-api`
- health check wiring
- runtime config injection
- TLS endpoint strategy
- deployment slots or revision strategy when provider supports it

The current module emits design metadata only.


# Dev Environment

Development environment design root.

This directory is safe to run `tofu init -backend=false`, `tofu validate`, and `tofu plan` against because it declares design modules only. No provider, backend, cloud account, or real resource exists yet.


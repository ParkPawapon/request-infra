# Production Environment

Production environment design root.

Production must be private-by-default, separated from staging/dev, and backed by a provider secret manager, audited access, backups, and incident runbooks before any deployment. This directory contains no provider-specific resources yet.


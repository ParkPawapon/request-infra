# Optional local-only overrides. Copy this file outside git-tracked paths before editing.
services:
  postgres:
    # Example: change only local port binding when another PostgreSQL is running.
    ports:
      - "127.0.0.1:15432:5432"

  redis:
    ports:
      - "127.0.0.1:16379:6379"

